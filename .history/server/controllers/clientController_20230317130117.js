import { successCode, failCode, errorCode } from "../utils/response.js";
import second from "../models/Product.js";

const getProduct = async (req, res) => {
  try {
    const products = await Product;
  } catch (error) {}
};
