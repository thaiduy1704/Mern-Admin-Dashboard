import React from "react";
import {
  Box,
  Divider,
  Typography,
  Drawer,
  IconButton,
  List,
  ListItem,
  ListItemButton,
  ListItemIcon,
  ListItemText,
  useTheme,
} from "@mui/material";

const SideBar = () => {
  return <div>SideBar</div>;
};

export default SideBar;
